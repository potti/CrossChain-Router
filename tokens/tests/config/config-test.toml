# router swap identifier, must have prefix 'routerswap'
Identifier = "routerswap#test"

# router swap type (eg. erc20swap, nftswap, anycallswap)
SwapType = "erc20swap"

# test module name
Module = "near"

# rpc listen port
Port = 11556

# use private key instead of mpc signing
SignWithPrivateKey = "ed25519:2eFH8pYc9xLiDvaqCL62XLXSQZNxD7uoGoq31mG1zQfE7DbV9gSXKG3qhxpgwWoii75T4MKqHKvy7B51VAsaAjpu"
SignerAddress = "userdemo.testnet"

# allow call into router from contract
AllowCallByContract = false

# is debug mode (print more logs)
IsDebugMode = true

# all chain ids (pass 'miss token config' checking)
AllChainIDs = ["1001313161555", "1337"]

[Gateway]
APIAddress = ["https://archival-rpc.testnet.near.org"]

# chain config
[Chain]
BlockChain = "near"
ChainID = "1001313161555"
Confirmations = 1
InitialHeight = 0
RouterContract = "r2.crossdemo.testnet"

# token config
[Token]
ContractAddress = "a1.crossdemo.testnet"
ContractVersion = 6
Decimals = 24
RouterContract = "r2.crossdemo.testnet"
TokenID = "anyBNB"
Underlying = "0xbb4cdb9cbd36b01bd1cbaebf2de08d9173bc095c"

[Swap]
BigValueThreshold = "10000000000000000000000000"
MaximumSwap = "10000000000000000000000000"
MaximumSwapFee = "0"
MinimumSwap = "1"
MinimumSwapFee = "0"
SwapFeeRatePerMillion = 0
